<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Analytics</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/url_analytics.css">
    <script src="/js/main.js" defer></script>
</head>
<body>
    <%- include('partials/header') %>
    <main>
        <h2 style="text-align: center; margin-bottom: 1.4em; color: rgba(0, 0, 0, 0.836);">Showing Analytics for <span style="color: tomato;">(<%= urlAnalytics.shortUrl %>)</span></h2>
        <div class="container">
            <% if (urlAnalytics.analytics && urlAnalytics.analytics.length > 0) { %>
                <table class="url-analytics-table">
                    <thead>
                        <th>IP Address</th>
                        <th>No. of Clicks</th>
                        <th>Country Code</th>
                        <th>Country</th>
                    </thead>
                    <tbody>
                        <% for (let i = 0; i < urlAnalytics.analytics.length; i++) { %>
                            <tr class="analytics-row">
                                <td><%= urlAnalytics.analytics[i].ip ? urlAnalytics.analytics[i].ip : '' %></td>
                                <td><%= urlAnalytics.analytics[i].clickCount ? urlAnalytics.analytics[i].clickCount : '' %></td>
                                <td><%= urlAnalytics.analytics[i].locationInfo.addressCountryCode
                                    ? urlAnalytics.analytics[i].locationInfo.addressCountryCode : '' %></td>
                                <td><%= urlAnalytics.analytics[i].locationInfo.addressCountry
                                    ? urlAnalytics.analytics[i].locationInfo.addressCountry : '' %></td>
                            </tr>
                        <% } %>
                    </tbody>
                    
                </table>
            <% } else { %>
                <p>Your URL has 0 traffick currently.</p>
            <% } %>
        </div>
        <div class="pagination">
            <a href="/urls/analytics/<%= urlAnalytics.analytics[0].urlCode %>/<%= urlAnalytics.previousPage %>"><< Previous</a> 
            <a href="/urls/analytics/<%= urlAnalytics.analytics[0].urlCode %>/<%= urlAnalytics.nextPage %>">Next >></a>
        </div>
    </main>
    <%- include('partials/footer') %>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URLs History</title>
</head>
<body>
    <%- include('partials/header') %>
    <main class="container">
        <h2>URLs History</h2>
        <% if (urlsHistory.urls && urlsHistory.urls.length > 0) { %>
            <% for (let i = 0; i < urlsHistory.urls.length; i++) { %>
                <div>
                    <div>
                        <h3>Description</h3>
                        <p><%= urlsHistory.urls[i].description ? urlsHistory.urls[i].description : 'Not Available' %></p>
                    </div>
                    <div>
                        <h3>Short URL</h3>
                        <p><%= urlsHistory.urls[i].shortUrl ? urlsHistory.urls[i].shortUrl : 'Not Available' %></p>
                    </div>
                    <div>
                        <h3>Date Created</h3>
                        <p><%= urlsHistory.urls[i].createdAt ? urlsHistory.urls[i].createdAt : 'Not Available' %></p>
                    </div>
                    <a href="/urls/view/<%= urlsHistory.urls[i]._id %>" class="view-url">View</a>
                    <a href="/urls/view/<%= urlsHistory.urls[i]._id %>" class="delete-url">Delete</a>
                </div><hr>
            <% } %>
        <% } else { %>
            <p>Your URLs history is currently empty.</p>
        <% } %>
    </main>
    <%- include('partials/footer') %>
</body>
</html>